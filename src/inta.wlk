/*
 * El archivo no compila tiene errores. De nuevo hay mensajes que no tiene objeto receptor del mansaje, parcela()solito sin obeto que reciba el mensaje
 * El inta esta incompleto
 * parcelas debía ser una coleccion
 */
import parcelas.*
object inta{
	var property parcela
	// method promedioPlantas(parcelas)=parcelas.plantas().sum() / parcela()
	method sustentable()=parcela>4
}